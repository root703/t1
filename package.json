{
  "name": "admin",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "docker:build": "docker compose -p instant-quote-admin build",
    "docker:deploy": "docker compose -f docker-compose.prod.yml up",
    "docker:down": "docker-compose -p instant-quote-admin down",
    "docker:up": "docker-compose -p instant-quote-admin up",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
